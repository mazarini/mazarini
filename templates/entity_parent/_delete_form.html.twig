<form method="post" action="{{ path('app_entity_parent_delete', {'id': entity_parent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity_parent.id) }}">
    <button class="btn">Delete</button>
</form>
