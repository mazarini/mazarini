<form method="post" action="{{ path('app_entity_child_delete', {'id': entity_child.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity_child.id) }}">
    <button class="btn">Delete</button>
</form>
